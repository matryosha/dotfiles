- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.tmux.conf.local: tmux.conf.local
    ~/.vim: vim
    ~/.vimrc: vimrc

- create:
    - ~/downloads
    - ~/.vim/undo-history

- shell:
  - [git submodule update --init --recursive, Installing submodules]
